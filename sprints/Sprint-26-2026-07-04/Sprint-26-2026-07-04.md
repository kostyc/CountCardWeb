# Sprint 26 — Expo Monorepo Migration

**Date**: 2026-07-04  
**Status**: In progress (Expo feature parity sprint)

## Deliverables (scaffold — initial)

- npm workspaces monorepo: `apps/web`, `apps/expo`, `packages/*`, `functions/`
- Shared packages: `@countcard/core`, `@countcard/firebase`, `@countcard/encryption`, `@countcard/api-client`, `@countcard/ui`
- Firebase Cloud Functions API (`functions/src/`) mirroring Next.js `/api/*` routes
- Expo Router app with auth (email, Apple iOS), tab navigation, recruits/count-cards lists
- Native modules: image picker, push notifications, biometrics (`apps/expo/lib/`)
- EAS Build config at repo root (`eas.json`)

## Continuation (2026-07-04)

- `EXPO_PUBLIC_*` synced from `.env.local`; Expo loads via `apps/expo/.env.local` symlink + `app.config.ts` dotenv
- Ported to Expo: profile wizard, count-card workflow actions/history, messaging tab + thread UI, admin role assignment
- **Ported (2026-07-04):** `recruits/create`, `count-cards/new` (Firestore client SDK; no API required)
- Google Sign-In via `expo-auth-session` (`lib/googleAuth.ts`) — requires `EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID`
- EAS project: `@warriorwaypoint/countcard` (`737c4f89-e7de-4cd9-a185-130ffe62259a`)

### API access (org policy blocker)

**Live API:** `https://api-x5hh3t2iwq-uc.a.run.app` (Gen2 Cloud Run).  
**Hosting proxy:** `https://countcard-94c5b.web.app/api/*` (rewrite deployed; see [CLOUD-RUN-API-ACCESS.md](./CLOUD-RUN-API-ACCESS.md)).

Org policy blocks `allUsers` `run.invoker` — unauthenticated mobile/API calls get **403** until org admin grants public invoker OR domain-restricted exception. Firebase Hosting rewrites also require public invoker on the underlying Cloud Run service.

**2026-07-04 attempt (×2):** `gcloud run services add-iam-policy-binding api ... --member=allUsers --role=roles/run.invoker` failed with `FAILED_PRECONDITION` (org policy `iam.allowedPolicyMemberDomains`). Health check still **403**. Org admin runbook: [ORG-ADMIN-CLOUD-RUN.md](./ORG-ADMIN-CLOUD-RUN.md). Current invoker IAM: service accounts only.

**2026-07-04 verification (post org-admin claim):** Health check **still 403** on both Hosting proxy and direct Cloud Run URL. `gcloud run services get-iam-policy api` shows **no `allUsers`** — only compute/App Engine/firebase-adminsdk service accounts. Retry of `add-iam-policy-binding ... allUsers` still **FAILED_PRECONDITION** (org policy). Org admin must complete Step 1 (project override for `iam.allowedPolicyMemberDomains`) before Step 2.

**2026-07-04 re-verification (Sprint continuation):** Both URLs still **403**. IAM unchanged (service accounts only). `gcloud run services add-iam-policy-binding ... allUsers` → `FAILED_PRECONDITION` (org policy `iam.allowedPolicyMemberDomains`). **Blocker unchanged** — org admin must complete [ORG-ADMIN-CLOUD-RUN.md](./ORG-ADMIN-CLOUD-RUN.md) Step 1 then Step 2 before any `@countcard/api-client` routes work. Firestore-direct screens (`recruits/create`, `count-cards/new`) unaffected.

**EAS iOS preview (2026-07-04):** `eas build --profile preview --platform ios --non-interactive` failed — *"couldn't find any credentials suitable for internal distribution"*. Must run **interactively** in Terminal (Apple ID / distribution cert).

**Android SHA-256 (2026-07-04):** `firebase apps:android:sha:list` for `1:36792557920:android:96374c98081665b85bbe05` → **no hashes** (expected until first EAS Android preview build).

### Cloud Run workaround (2026-07-04)

Org policy still blocks `/api/health` (403). **Expo no longer depends on HTTP API** for core flows:

- Firestore rules deployed: `recruits`, `countCards`, admin `userProfiles` access
- Firestore trigger deployed: `syncUserClaimsOnProfileWrite` (custom claims from profile writes)
- Expo ported: count-card workflow → `@countcard/firebase`; admin → `listUserProfiles` + `updateUserProfile`

Runbook: [CLOUD-RUN-WORKAROUND.md](./CLOUD-RUN-WORKAROUND.md). Web encryption/export still need org-policy fix or future port.

**Env updated:** `.env.local` now has `EXPO_PUBLIC_API_BASE_URL` and `NEXT_PUBLIC_API_BASE_URL` = `https://countcard-94c5b.web.app`. Expo restarted.

Functions Express app now mounts routes at `/api/*` (matches `@countcard/api-client` paths).

### Google Sign-In

`EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID` set from Firebase Identity Toolkit (`36792557920-can8v6ie8fpubvjb7497vbgib4qadinq.apps.googleusercontent.com`).

Restart Expo after `.env.local` changes.

### EAS preview build

**Prerequisite (2026-07-04):** Push `EXPO_PUBLIC_*` to EAS preview (local `.env.local` is not on build servers):

```bash
cd apps/expo
grep -E '^(EXPO_PUBLIC_|EAS_PROJECT_ID=)' ../../.env.local > /tmp/countcard-expo-eas.env
eas env:push preview --path /tmp/countcard-expo-eas.env --force
```

Eight vars uploaded to preview: Firebase client config, `EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID`, `EXPO_PUBLIC_API_BASE_URL`, `EAS_PROJECT_ID`.

Run **interactively** (Apple credentials required):

```bash
cd apps/expo && eas build --profile preview --platform ios
```

Non-interactive mode fails without stored iOS credentials for internal distribution.

**2026-07-04:** `eas build --profile preview --platform ios --non-interactive` → *"couldn't find any credentials suitable for internal distribution"*. Run interactively in Terminal (Apple ID / distribution cert required).

**Google Sign-In (EAS preview):** Register redirect URI `countcard://oauth` in Google Cloud Console → Credentials → OAuth 2.0 Web client (same `EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID`). Optional: set `EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID` / `EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID` in EAS preview env.

### iOS simulator manual test (Expo Go)

Expo dev server on `:8081` (if running). Simulator: **iPhone 17 Pro-Go**.

```bash
cd apps/expo && npx expo start --ios --port 8081
```

**Limits in Expo Go:** `@react-native-firebase/app-check` not available (dev uses debug token path). Google Sign-In needs `EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID` + redirect URI registered. For full native parity, use EAS **development** or **preview** build.

| Flow | Route | Pass criteria |
|------|-------|---------------|
| Google Sign-In | `/(auth)/login` | OAuth completes; lands on tabs |
| Profile encryption badge | `/profile` | StatusBadge shows **Compatible** |
| Create recruit | `/recruits/create` | Form saves to Firestore |
| New count card | `/count-cards/new` | Form saves to Firestore |
| Count card workflow | `/count-cards/[id]` | Approve / reject / consolidate actions persist |
| Admin role assign | `/admin` | Role change saves; wait ~1.5s; refresh token for claims |

### EAS preview manual test (device)

After iOS preview install from EAS dashboard:

| Flow | Route | Pass criteria |
|------|-------|---------------|
| Google Sign-In | `/(auth)/login` | OAuth completes; lands on tabs |
| Profile encryption badge | `/profile` | StatusBadge shows **Compatible** |
| Create recruit | `/recruits/create` | Form saves to Firestore |
| New count card | `/count-cards/new` | Form saves to Firestore |
| Count card workflow | `/count-cards/[id]` | Workflow actions update status in Firestore |
| Admin role assign | `/admin` | `updateUserProfile` + trigger syncs custom claims |

### App Check (EAS production)

- Dev: `EXPO_PUBLIC_APPCHECK_DEBUG_TOKEN` + JS SDK debug provider
- Production EAS: `@react-native-firebase/app-check` wired in `apps/expo/lib/appCheck.ts` + config plugins
- **API verified (2026-07-04):** App Check configs exist via REST (`appAttestConfig` + `playIntegrityConfig` on project `36792557920`). Confirm in [Firebase App Check console](https://console.firebase.google.com/project/countcard-94c5b/appcheck) that both apps show as registered.
- **REST re-verified (2026-07-04):** iOS `appAttestConfig` (`tokenTtl: 3600s`); Android `playIntegrityConfig` (`tokenTtl: 3600s`, `minDeviceRecognitionLevel: NO_INTEGRITY`). Firebase apps: CountCard iOS (`7815c2b…`), CountCard Android (`96374c98…`), Count Card web.
- **Android SHA-256:** still none (`firebase apps:android:sha:list` empty). No EAS builds yet — run Android preview build or `eas credentials:configure-build -p android -e preview` interactively, then `firebase apps:android:sha:create ANDROID_APP_ID SHA256`.
- **Done:** `GoogleService-Info.plist` / `google-services.json` in `apps/expo/`; iOS/Android Firebase apps created; `app.config.ts` references both + `ITSAppUsesNonExemptEncryption: false`; `firebaseappcheck.googleapis.com` enabled on project.

## Next.js retirement criteria

See [EXPO-WEB-PARITY.md](./EXPO-WEB-PARITY.md) — retire `apps/web` only when all checklist items pass.

## Commands

```bash
npm run dev:web      # Next.js at :3000
npm run dev:expo     # Expo dev server
npm run build:web
npm run build:expo
cd functions && npm run deploy
firebase deploy --only functions,hosting
eas build --profile preview
```
