# Legacy hosting stub (superseded)

Firebase Hosting now serves the Expo web export from `apps/expo/dist` (see root `firebase.json` `hosting.predeploy`).

This folder is kept as a minimal API landing reference only. Deploy uses `npm run build:expo`, not these files.
