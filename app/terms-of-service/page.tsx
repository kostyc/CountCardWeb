'use client';

/**
 * Terms of Service – placeholder page (Sprint 2).
 * Linked from home and signup; full document content to be added.
 */

import UnderConstructionPlaceholder from '@/components/feedback/UnderConstructionPlaceholder';

export default function TermsOfServicePage(): JSX.Element {
  return <UnderConstructionPlaceholder showBackLink={false} />;
}
