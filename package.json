{
  "name": "countcard",
  "version": "2026.0.2",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "functions"
  ],
  "scripts": {
    "dev:web": "npm run dev -w countcard-web",
    "dev:expo": "npm run start -w countcard-expo",
    "build:web": "npm run build -w countcard-web",
    "build:expo": "npm run build -w countcard-expo",
    "lint": "npm run lint -w countcard-web",
    "build": "npm run build:web"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  }
}
